<%- include('../layouts/main', { content: `
<style>
.blog-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 2rem 1rem;
}

.category-header {
  text-align: center;
  margin-bottom: 3rem;
  padding: 3rem 2rem;
  background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
  color: white;
  border-radius: 12px;
}

.category-header h1 {
  font-size: 2.5rem;
  margin-bottom: 1rem;
}

.category-header p {
  font-size: 1.2rem;
  opacity: 0.95;
}

.blog-layout {
  display: grid;
  grid-template-columns: 1fr 350px;
  gap: 3rem;
}

.blog-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 2rem;
}

.blog-card {
  background: white;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  transition: transform 0.3s, box-shadow 0.3s;
  text-decoration: none;
  color: inherit;
  display: block;
}

.blog-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 4px 16px rgba(0,0,0,0.15);
}

.blog-card-image {
  width: 100%;
  height: 200px;
  object-fit: cover;
  background: #f0f0f0;
}

.blog-card-content {
  padding: 1.5rem;
}

.blog-card-title {
  font-size: 1.25rem;
  margin-bottom: 0.75rem;
  color: #2c3e50;
  line-height: 1.4;
}

.blog-card-excerpt {
  color: #7f8c8d;
  font-size: 0.95rem;
  line-height: 1.6;
  margin-bottom: 1rem;
}

.blog-card-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 0.85rem;
  color: #95a5a6;
  border-top: 1px solid #ecf0f1;
  padding-top: 0.75rem;
}

.pagination {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 1rem;
  margin-top: 3rem;
}

.pagination a,
.pagination span {
  padding: 0.5rem 1rem;
  border: 1px solid #ddd;
  border-radius: 4px;
  text-decoration: none;
  color: #2c3e50;
}

.pagination a:hover {
  background: #3498db;
  color: white;
  border-color: #3498db;
}

.pagination .current {
  background: #3498db;
  color: white;
  border-color: #3498db;
}

.no-posts {
  text-align: center;
  padding: 4rem 2rem;
  color: #7f8c8d;
}

.no-posts h2 {
  font-size: 1.5rem;
  margin-bottom: 1rem;
}

@media (max-width: 968px) {
  .blog-layout {
    grid-template-columns: 1fr;
  }

  .blog-grid {
    grid-template-columns: 1fr;
  }

  .category-header h1 {
    font-size: 1.8rem;
  }
}
</style>

<div class="blog-container">
  <div class="category-header" style="<%= category?.color ? 'background: linear-gradient(135deg, ' + category.color + ' 0%, ' + category.color + 'dd 100%)' : '' %>">
    <h1>
      <% if (category?.icon) { %><%= category.icon %> <% } %>
      <%= category?.name || 'Categorie' %>
    </h1>
    <% if (category?.description) { %>
      <p><%= category.description %></p>
    <% } %>
  </div>

  <div class="blog-layout">
    <div class="blog-main">
      <% if (posts && posts.length > 0) { %>
        <div class="blog-grid">
          <% posts.forEach(post => { %>
            <a href="/blog/<%= post.slug %>" class="blog-card">
              <% if (post.featuredImage) { %>
                <img src="<%= post.featuredImage %>" alt="<%= post.title %>" class="blog-card-image">
              <% } else { %>
                <div class="blog-card-image"></div>
              <% } %>

              <div class="blog-card-content">
                <h2 class="blog-card-title"><%= post.title %></h2>
                <p class="blog-card-excerpt"><%= post.excerpt %></p>

                <div class="blog-card-meta">
                  <span>📖 <%= post.readingTime %> min citire</span>
                  <span>👁️ <%= post.views %> vizualizări</span>
                </div>
              </div>
            </a>
          <% }) %>
        </div>

        <!-- Pagination -->
        <% if (pagination.totalPages > 1) { %>
          <div class="pagination">
            <% if (pagination.page > 1) { %>
              <a href="?page=<%= pagination.page - 1 %>">« Înapoi</a>
            <% } %>

            <span class="current">Pagina <%= pagination.page %> din <%= pagination.totalPages %></span>

            <% if (pagination.page < pagination.totalPages) { %>
              <a href="?page=<%= pagination.page + 1 %>">Înainte »</a>
            <% } %>
          </div>
        <% } %>
      <% } else { %>
        <div class="no-posts">
          <h2>Nu sunt articole în această categorie</h2>
          <p>Revino mai târziu pentru a descoperi conținut nou!</p>
          <a href="/blog" style="display: inline-block; margin-top: 1rem; padding: 0.75rem 1.5rem; background: #3498db; color: white; text-decoration: none; border-radius: 4px;">Vezi toate articolele</a>
        </div>
      <% } %>
    </div>

    <!-- Sidebar -->
    <%- include('./partials/sidebar', { categories, popularPosts: [], tags: [], filters: {} }) %>
  </div>
</div>
` }) %>
